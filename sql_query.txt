We will fix ONE thing first:

👉 Replace unsafe queries

Example you have:

$conn->query("SELECT * FROM comments WHERE message_id = {$row['id']}");
✅ Replace with:
$stmt = $conn->prepare("SELECT * FROM comments WHERE message_id = ?");
$stmt->bind_param("i", $row['id']);
$stmt->execute();
$comments = $stmt->get_result();



///////////////////////
echo "<a href='{$base}?page=$i'>$i</a>";